<div class="topBackPic"></div>

<h1>Main page is commin soon ...</h1>

<div class="row justify-content-evenly">
    <div class="col-sm-5 my-5">
        <a href="/allpets" class="w-100 btn btn-lg btn-success" type="submit">Adopt a Pet</a>
    </div>
    <div class="col-sm-5 my-5">
        <a href="/allpets" class="w-100 btn btn-lg btn-success" type="submit">Find New Family For Pet</a>
    </div>
</div>
<br>
<br>



{{!-- {{#each allInfo}}
<div class="card text-dark bg-light my-3 border border-dark">
    <a href="/addcomment/post/{{id}}" class="d-inline-flex card-header p-2 justify-content-between"
        style="text-decoration: none;">
        <div class="me-3"><strong>{{title}}</strong></div>
        <div class="ms-3">Posted By <strong>{{user.username}}</strong> on {{format_date createdAt}} {{format_time
            createdAt}}</div>
    </a>
    <div class="card-body">
        <p class="card-text">{{text}}</p>
    </div>
</div>
{{/each}} --}}